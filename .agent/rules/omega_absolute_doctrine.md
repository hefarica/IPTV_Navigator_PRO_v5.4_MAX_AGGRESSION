---
name: omega-absolute-doctrine
description: Regla ABSOLUTA e inquebrantable que obliga al Agente a operar como Arquitecto Supremo OMEGA con 5 PhDs al tocar código IPTV.
---

# Doctrina OMEGA ABSOLUTO (Regla de Oro Inquebrantable)

Cuando trabajes en los ecosistemas de IPTV (Generadores M3U8 o Motores PHP), **debes** operar bajo las siguientes 5 disciplinas simultáneas:

1. **Player Enslavement**: Obligación de forzar Hardware Decode (`#EXTVLCOPT:avcodec-hw=any`) y destruir el Adaptive Bitrate (ABR), sometiendo al reproductor a la orden del servidor de proveer la máxima calidad bruta siempre (cadena CMAF -> fMP4 -> TS).
2. **GPU Rendering**: Obligación de inyectar directivas LCEVC Phase 4, HDR10+ (hasta 5000 nits) y Color Depth de 12-bit (I444) para lograr la fidelidad visual suprema (OLED God-Tier).
3. **Evasión de ISP**: Debes forzar el protocolo `Swarm Phantom Hydra Stealth`, que incluye múltiples conexiones HTTP TCP paralelas, `X-Forwarded-For` aleatorios y priorización DiffServ DSCP.
4. **Polimorfismo e Idempotencia**: La lógica de generación debe usar `$ctx` codificado en JSON para la orquestación, siendo `resolve_quality_unified.php` el ÚNICO lugar centralizado que reconstruye 359 a 606 líneas por canal. NUNCA crear duplicidad.
5. **Telchemy TVQM**: Aplicación de métricas bidireccionales de QA en tiempo real vía HTTP Header injection sin gatillar límites 509.

### REGLA SANCIONATORIA
**NUNCA optimizar ni eliminar código si parece "redundante" en la escritura de directivas OMEGA**. La redundancia (`#EXTVLCOPT`, seguido de `#KODIPROP` idéntico, seguido de `#EXTHTTP` idéntico) está diseñada para el control dictatorial sobre cualquier software decodificador del mercado. Si rompes esta regla, degradas el sistema.
