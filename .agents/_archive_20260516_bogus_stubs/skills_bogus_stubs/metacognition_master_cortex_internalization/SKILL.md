---
name: "Metacognition: Master Cortex Internalization — Sincronización Total con el Alma del Proyecto"
description: "Protocolo obligatorio de inicialización cognitiva. Antes de cualquier acción, el agente DEBE sincronizarse con la jerarquía de reglas y skills del directorio .agent."
---

# Master Cortex Internalization

## La Regla de Oro

**Lo que está escrito en el directorio `.agent` es la Verdad Absoluta.** Ignorar un protocolo, workflow o skill documentado es una falla crítica de razonamiento.

## El Protocolo de Inicialización Forzada (Capa Táctica)

Esta habilidad es un **Mandato Operativo**. Antes de cualquier acción compleja, la lógica del agente DEBE ejecutar un escaneo mental de 5 capas:

1. **Capa Inicial**: `C:\Users\HFRC\Desktop\IPTV_Navigator_PRO_v5.4_MAX_AGGRESSION\.agent\skills` (Directorio base de conocimiento).
2. **Capa 0 (Reglas Globales)**: Reglas Inmutables (ej. `user_global`, `mandatory_skills_activation`).
3. **Capa 1 (Arquitectura)**: Arquitectura SSOT y OMEGA (ej. `ssot-resolve-unified`, `omega_absolute_doctrine`).
4. **Capa 2 (Blindaje)**: Blindaje de Seguridad (SSRF, 403, 509).
5. **Capa 3 (Ejecución)**: Workflows de Ejecución aplicables a la tarea.

## Internamiento de Datos (Internalization)

- **No asumas**, verifica: Aunque creas recordar cómo funciona el `sniper_mode`, vuelve a leer el archivo si han pasado más de 10 mensajes.
- **La palabra es ley**: Si un archivo dice "MANDATORY", no es una sugerencia.
- **Detección de Amnesia**: Si te encuentras sugiriendo algo que contradice un archivo en `.agent`, detente y corrige tu curso inmediatamente.

### Ejemplo de Aplicación

#### Contexto de Ejemplo

Usuario pide: "Arregla el error 403 en este canal"

1. **Paso 1 (Cortex)**: Busco en `.agent/skills` por "403".
2. **Paso 2**: Encuentro `anti_403_url_parameter_pollution` y `metacognition_root_cause_not_symptom`.
3. **Paso 3**: Aplico el `Backward Chain Tracing` antes de proponer un fix.
4. **Paso 4**: Verifico si el fix requiere `302 Redirect` o `200OK Proxy` según `omega_absolute_doctrine`.

---

> [!IMPORTANT]
> **La eficiencia no proviene de la velocidad, sino de la precisión arquitectónica.**
> Un agente que internaliza el `.agent` no comete errores de regresión.
