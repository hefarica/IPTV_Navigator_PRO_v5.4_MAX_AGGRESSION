# ═══════════════════════════════════════════════════════════════════════════
# NET SHIELD Fase 3.5 Phase 2 — Lua Circuit Breaker per upstream host
# ───────────────────────────────────────────────────────────────────────────
# Estado in-memory por worker → multi-worker requires shared dict.
# 20MB ≈ ~250k entries (key=host, value=state JSON).
#
# MEJORA 2026-04-26: Agregado circuit_metrics para telemetría dedicada
#   de trips, recoveries, probe failures, open rejections.
#   Ref: Plan Técnico §0 (lua_shared_dict circuit_metrics 5m)
# ═══════════════════════════════════════════════════════════════════════════
lua_shared_dict upstream_state   20m;
lua_shared_dict circuit_metrics  5m;

