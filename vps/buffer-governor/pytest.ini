[pytest]
# Defecto C (council audit): el suite usa @pytest.mark.asyncio y un fixture async.
# asyncio_mode=auto hace que pytest-asyncio trate tests Y fixtures async sin
# necesitar decoradores extra, de modo que la suite realmente corra (no quede skip).
asyncio_mode = auto
testpaths = tests
